subprojects {
    apply plugin: 'idea'

    group 'io.netifi.proteus.chatDemo'
    version '1.0-SNAPSHOT'

    ext {
        proteusVersion = '0.7.18'
    }
    repositories {
        mavenLocal()
        jcenter()
    }
}